systemLog:
   verbosity: 0
   quiet: false
   path: "{{ path }}/replset/rs0/rs0.log"
   logAppend: true
   destination: file
   timeStampFormat: iso8601-utc

processManagement:
   fork: true
   pidFilePath: "{{ path }}/replset/rs0/rs0.pid"

net:
   port: 27027
   bindIp: 127.0.0.1
   maxIncomingConnections: 65536

storage:
   dbPath: "{{ path }}/replset/rs0/data/"
   directoryPerDB: true
   indexBuildRetry: true
   preallocDataFiles: true
   smallFiles: true
   #repairPath: "{{ path }}/replset/rs0/data/repair/_tmp"
   journal:
      enabled: true

operationProfiling:
   slowOpThresholdMs: 100
   mode: slowOp

replication:
   oplogSizeMB: 10
   replSetName: "replset"

#sharding:
#   clusterRole: <string>

# mongos-only
#sharding:
#   configDB: <string>
#   chunkSize: <int>
